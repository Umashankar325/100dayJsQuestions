console.log("Accept the length and width of a rectangle. Calculate & print the area and perimiter.");
let length=+prompt("length of a rectangle");
let width=+prompt("width of a rectangler");
let aria=length*width;
let perimiter=(length+width)*2;

console.log(`the aria of rectangle is ${aria} and parameter of rectangle is ${perimiter}`);